<%# vim: filetype=eruby.html
%>

<% if object.attributes.size > 0 %>
  <h2>Attributes Defined in Cookbook</h2>
  <ul class="summary">
    <% object.attributes.select { |o| o.type == :cookbookattribute }.each do |attribute| %>
      <li>
        <span class="summary_signature">
          <strong><a href="#<%= attribute.namespace_to_anchor() %>"><%= attribute.namespace_to_node() %></a></strong>
        </span>
      </li>
    <% end %>
  </ul>
<% end %>
